// import HomePage from '@/components/pages/landing/HomePage';
// test('render a hero section in landing page', () => {
//     render(<HomePage />)
// })

// // 👇 you need these mocks if HomePage uses next/navigation or next/link
// jest.mock("next/navigation", () => ({
//     useRouter() {
//         return {
//             push: jest.fn(),
//             replace: jest.fn(),
//             refresh: jest.fn(),
//         };
//     },
// }));

// test("render a hero section in landing page", () => {
//     render(<HomePage />);

//     // example: check existence of some text in hero
//     const heroText = screen.getByText(/welcome/i);
//     expect(heroText).toBeInTheDocument();
// });
